/*
  OPTIONAL SHARED FIREBASE CONFIG
  Paste the Firebase Web App config object below once before deploying this folder.
  It is safe for Firebase web config to be public; security is enforced by Authentication + Firestore Rules.

  Example:
  window.PM_FIREBASE_CONFIG = {
    apiKey: "...",
    authDomain: "your-project.firebaseapp.com",
    projectId: "your-project",
    storageBucket: "your-project.appspot.com",
    messagingSenderId: "...",
    appId: "..."
  };
*/
window.PM_FIREBASE_CONFIG = window.PM_FIREBASE_CONFIG || null;
